@{
    scopedParameters = @{
        'machines*' = @{
            MachineID = @{
                Alias = 'Id'
            }
        }
		'recommendation*' = @{
			RecommendationID = @{
				Alias = 'Id'
			}
		}
		'software*' = @{
			SoftwareID = @{
				Alias = 'Id'
			}
		}
    }
}